<?php

use App\Models\User;

/*it('can create an idea', function () {
        $this->browse(function (Browser $browser) {
        $browser->loginAs(User::factory()->create())
            ->visit('/idea')
            ->click('@create-idea-button')
            ->assertPathIs('/idea/create')
            ->assertSee('Create Idea');
    });
});*/
